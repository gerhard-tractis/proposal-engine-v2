---
title: 'Proposal Engine Refactor — HTML-first Architecture'
slug: 'proposal-engine-html-refactor'
created: '2026-02-15'
status: 'completed'
stepsCompleted: [1, 2, 3, 4]
completedDate: '2026-02-15'
tech_stack: ['Next.js 16.1.6', 'React 19', 'Supabase (Postgres + Storage)', 'Tailwind CSS 3.4', 'LangChain + Sonnet 4.5', 'Dembrandt CLI', 'TypeScript ESM', 'Turborepo + pnpm', 'Zod 3.23']
files_to_modify: ['supabase/migrations/003_html_proposal_schema.sql', 'apps/web/src/app/proposals/[slug]/[token]/route.ts', 'apps/web/src/app/proposals/[slug]/[token]/page.tsx', 'apps/web/src/lib/proposal-helpers.ts', 'apps/web/src/middleware.ts', 'scripts/generate-proposal.ts', 'scripts/lib/brand.ts', 'scripts/lib/assets.ts', 'scripts/lib/tailwind.ts', 'scripts/package.json', 'apps/agent/prompts/agent-1-brand-designer.md', 'apps/agent/prompts/agent-2-architect.md', 'apps/agent/prompts/agent-3-html-builder.md', 'apps/agent/prompts/agent-4-polish.md', 'packages/shared/src/types/proposal.ts']
code_patterns: ['Supabase admin client via getSupabaseAdmin() singleton', 'Zod schema validation on read/write', 'Next.js App Router async params', 'pnpm monorepo with @repo/ imports', 'Middleware token validation at /proposals/* path using anon client + RLS', 'Dembrandt CLI as apps/agent dependency', 'CSS vars generated from client colors (branding.ts)', 'save-proposal.ts: read JSON → Zod validate → Supabase insert']
test_patterns: ['Vitest for component tests (apps/web/src/components/proposal/__tests__/)']
---

# Tech-Spec: Proposal Engine Refactor — HTML-first Architecture

**Created:** 2026-02-15

## Overview

### Problem Statement

The current system requires Vercel redeploys for every component change, is limited to generic React block components, has poor brand color extraction (scrapes UI noise like grays and state colors), and produces visually similar proposals regardless of content. The architecture tightly couples proposal rendering to the Next.js app.

### Solution

Replace React-rendered proposals with self-contained HTML files generated by a 4-agent pipeline (Brand Designer → Architect → HTML Builder → Polish). HTML is stored in Supabase Storage and served through a lightweight Next.js proxy at the existing `/proposals/` route. Tailwind classes are compiled locally before upload. No redeploy needed for new proposals.

### Scope

**In Scope:**
- New DB migration (add `client_name`, `client_url`, `view_count`, `expires_at`, `html_path`, `asset_manifest`; update RLS policies for new status values)
- Supabase Storage buckets (`proposals`, `proposal-assets`) — created via Supabase Dashboard/CLI, NOT raw SQL
- `increment_view` DB function
- HTML proxy route handler + old-proposal redirect strategy
- `injectSignedUrls()` utility
- `scripts/generate-proposal.ts` pipeline script with `scripts/package.json`
- 4 agent system prompts (Brand Designer, Architect, HTML Builder, Polish)
- Tailwind CLI standalone compilation step (cross-platform)
- `scrapeBrand()` and `uploadClientAssets()` utilities
- CSP headers for HTML proposal security

**Out of Scope:**
- Removing old React block components (post-migration cleanup)
- Admin dashboard changes
- Deleting old agent prompts (2a, 2b, 3)
- Updating `CLAUDE.md`, `README.md`, `PROJECT_STATE.md`

## Context for Development

### Codebase Patterns

- **Supabase client**: Two-tier — `getSupabaseClient()` (anon, Edge-compatible) and `getSupabaseAdmin()` (service role, server-only). Both are lazy singletons in `apps/web/src/lib/supabase.ts`.
- **RLS policies**: Current anon policy is `status = 'published'` only. The middleware uses the anon client (`getSupabaseClient()`), so it is subject to RLS. Any new status values that should be accessible must have RLS policies updated.
- **Zod validation**: Schemas in `packages/shared/src/types/proposal.ts` are single source of truth. Validated on both save (`scripts/save-proposal.ts`) and read (`proposal-helpers.ts`).
- **Next.js App Router**: Async `params` pattern (Next.js 16). `dynamic = 'force-dynamic'`, `revalidate = 0` on proposal pages. **Critical: `route.ts` and `page.tsx` CANNOT coexist in the same directory.**
- **Middleware**: `apps/web/src/middleware.ts` validates tokens at `/proposals/*` using `getSupabaseClient()` (anon key, subject to RLS). Rate limiting (10/min/IP), format validation, fallback env var tokens. Adds `X-Robots-Tag: noindex`.
- **Branding**: `branding.ts` generates 24+ CSS variables from proposal `client.colors`. Hex→HSL conversion for Tailwind compatibility. Applied in proposal `layout.tsx`.
- **Proposal layout**: `proposals/[slug]/[token]/layout.tsx` fetches full proposal, applies CSS vars, renders sticky header with logo/title. Supports 3 header styles.
- **Dembrandt**: `dembrandt` is a dependency of `apps/agent` (not globally installed). CLI command extracts hex colors via regex + Puppeteer favicon extraction. Currently in `apps/agent/src/routes/extract-design.ts`.
- **Save flow**: `scripts/save-proposal.ts` reads JSON from CLI arg → Zod validates → inserts to Supabase via admin client (env from `apps/agent/.env`). Scripts directory has no `package.json`.
- **Monorepo**: Turborepo + pnpm workspaces. `@repo/shared` for types. Build depends on `^build`.
- **No Supabase Storage usage exists** — clean slate for bucket setup.
- **Platform**: Windows 11. Shell commands must be cross-platform (no `/dev/null`, use platform-agnostic alternatives).

### Files to Reference

| File | Purpose |
| ---- | ------- |
| `supabase/migrations/001_initial_schema.sql` | Current DB schema, RLS policies (anon reads `status = 'published'` only) |
| `supabase/migrations/002_block_schema.sql` | V2 migration: added metadata + blocks JSONB columns |
| `apps/web/src/app/proposals/[slug]/[token]/page.tsx` | Current React rendering page |
| `apps/web/src/app/proposals/[slug]/[token]/layout.tsx` | Current branding/header layout |
| `apps/web/src/lib/proposal-helpers.ts` | Supabase query helpers — `getProposalBySlugAndToken` filters `status = 'published'` |
| `apps/web/src/lib/supabase.ts` | Supabase client setup (anon + admin singletons) |
| `apps/web/src/lib/branding.ts` | CSS variable generation from client colors |
| `apps/web/src/middleware.ts` | Token validation using **anon client** (subject to RLS), rate limiting |
| `packages/shared/src/types/proposal.ts` | Zod schemas: Proposal, Client, Block, etc. |
| `scripts/save-proposal.ts` | Current save script (pattern for generate script) |
| `apps/agent/prompts/create-proposal-command.md` | Current slash command (5-phase process) |
| `apps/agent/src/routes/extract-design.ts` | Dembrandt + Puppeteer extraction endpoint |
| `apps/web/src/components/proposal/blocks/index.ts` | Block component registry |
| `apps/agent/package.json` | Contains `dembrandt` and `puppeteer` dependencies |

### Technical Decisions

- **Keep `/proposals/` URL prefix** — new proxy lives at same path for consistency
- **Incremental migration** — new pipeline runs alongside old system; old React rendering stays until proven
- **Move old page.tsx to `/proposals/[slug]/[token]/legacy/`** — since `route.ts` and `page.tsx` cannot coexist, move old rendering to a sub-route. The route handler redirects old proposals (no `html_path`) to `/proposals/{slug}/{token}/legacy` internally.
- **Update RLS policies** — add new anon read policy for `status IN ('published', 'sent', 'viewed')` so middleware can validate tokens for HTML proposals without switching to admin client
- **Middleware uses admin client** — switch middleware from `getSupabaseClient()` (anon) to `getSupabaseAdmin()` (service role) to bypass RLS entirely. This is simpler than maintaining RLS policies for every new status value. The middleware already runs server-side only.
- **Tailwind CLI v3** — use `npx tailwindcss` (NOT `@tailwindcss/cli` which is v4 only). Provide a minimal standalone `tailwind.config.js` with no custom theme (agents use only default Tailwind utilities). Cross-platform: use temp file instead of `/dev/null`.
- **Sonnet 4.5 for all 4 agents** — specialized agents with reduced scope, better quality than single Opus pass, ~$0.37/proposal
- **Asset placeholders with manifest** — HTML uses `{{logo:client}}`, `{{logo:tractis}}`, `{{favicon:client}}`. Asset paths (with extensions) stored in DB column `asset_manifest JSONB` at upload time, so `injectSignedUrls` knows exact paths without listing buckets.
- **Scripts package.json** — create `scripts/package.json` with own dependencies (`@anthropic-ai/sdk`, `nanoid`, `@supabase/supabase-js`, `dotenv`) so scripts are self-contained. Run via `npx tsx scripts/generate-proposal.ts`.
- **Dembrandt via npx** — run as `npx --prefix apps/agent dembrandt <url>` to use the version installed in `apps/agent` without global install. Skip Puppeteer favicon extraction in scripts (use Dembrandt output only); favicon can be provided via `--favicon <url>` CLI arg or auto-detected from `<link rel="icon">` via simple HTML fetch + regex (no Puppeteer needed).
- **CSP headers** — serve HTML proposals with `Content-Security-Policy: default-src 'self' 'unsafe-inline'; img-src * data:; font-src *` to prevent malicious script injection from LLM-generated content.
- **LangChain for agent calls** — use existing LangChain + `@langchain/anthropic` (already in `apps/agent`) instead of introducing a second AI SDK. Import from `apps/agent` or duplicate the dependency in `scripts/package.json`.

## Implementation Plan

### Tasks

#### Phase 0: Shared Types (prerequisite for Phase 3)

- [ ] Task 1: Add HTML proposal types to shared package
  - File: `packages/shared/src/types/proposal.ts`
  - Action: Add new schemas and types for the HTML pipeline (keep existing schemas for backward compat):
    - `DesignSystemSchema` — Zod schema matching Agent 1 output: `{ brandColors: { primary, secondary?, accent? }, backgrounds: { base, surface, elevated }, text: { primary, secondary, muted }, borders: string, gradients: string[], shadows: string[], typography: { heading: string, body: string }, mood: z.enum(['corporate','tech','premium','industrial','friendly']), theme: z.enum(['dark','light']), logo: { url: string, transparentBg: boolean, bestOn: z.enum(['dark','light']) } }`
    - `SectionBlueprintSchema` — Zod schema matching Agent 2 output: `{ id: string, title: string, visualPattern: string, animations: string[], colorScheme: z.enum(['brand','inverted','gradient','subtle']), contentSummary: string, order: number }`
    - `AssetManifestSchema` — Zod schema: `{ clientLogo: string, clientFavicon?: string, tractisLogo: string }` (stores Storage paths with extensions)
    - `HtmlProposalRecordSchema` — Zod schema for the new DB row shape: `{ slug, token, client_name, client_url?, status: z.enum(['draft','published','sent','viewed','accepted','rejected']), view_count, expires_at?, html_path, asset_manifest, created_at }`
    - Export all derived TypeScript types
  - Notes: These schemas are used by `generate-proposal.ts` to validate agent outputs between pipeline steps. MUST be implemented before Phase 3.

#### Phase 1: Database & Storage Infrastructure

- [ ] Task 2: Create DB migration for HTML proposal schema
  - File: `supabase/migrations/003_html_proposal_schema.sql`
  - Action: Create new migration that:
    - Adds columns to `proposals` table: `client_name TEXT NOT NULL DEFAULT ''`, `client_url TEXT`, `view_count INT DEFAULT 0`, `expires_at TIMESTAMPTZ`, `html_path TEXT` (Storage path reference), `asset_manifest JSONB DEFAULT '{}'`
    - Adds CHECK constraint for `status` allowed values: `draft`, `published`, `sent`, `viewed`, `accepted`, `rejected`
    - Does NOT drop `client`, `metadata`, `blocks` columns (incremental — old proposals still need them)
    - Creates `increment_view` function: updates `view_count + 1` and sets status from `sent` → `viewed`
    - Adds index on `html_path` for Storage lookups
    - **Updates RLS policy**: Replace the existing anon read policy with `status IN ('published', 'sent', 'viewed')` to allow middleware (anon client) to validate tokens for both old and new proposals
  - Notes: The `html_path` column being NULL vs non-NULL is the discriminator between old (React) and new (HTML) proposals. The `asset_manifest` JSONB stores exact Storage paths with file extensions so `injectSignedUrls` doesn't need to list/guess.

- [ ] Task 3: Create Supabase Storage buckets
  - Action: Create buckets via **Supabase Dashboard** (production) or **`supabase/config.toml`** (local dev). Do NOT use raw SQL `INSERT INTO storage.buckets` as it can conflict with Supabase's internal state on hosted instances.
    - `proposals` bucket (private) — stores `{slug}.html` files
    - `proposal-assets` bucket (private) — stores client logos, favicons
    - RLS: service role only (no anon access — proxy serves via signed URLs)
  - Notes: For local dev, add bucket definitions to `supabase/config.toml` under `[storage]`. For production, create via Dashboard or `supabase storage create` CLI. Document both approaches in the migration file as comments.

- [ ] Task 4: Update middleware for dual-mode proposals
  - File: `apps/web/src/middleware.ts`
  - Action: Two changes:
    1. **Switch from anon to admin client**: Replace `getSupabaseClient()` with `getSupabaseAdmin()` in the `validateToken` function. This bypasses RLS entirely, so the middleware works with any status value without RLS policy changes. The middleware already runs server-side only (Edge Runtime), so using the service role key is safe.
    2. **Expand status filter**: Change `.eq('status', 'published')` to `.in('status', ['published', 'sent', 'viewed', 'draft'])` to allow access to HTML proposals in any active status.
  - Notes: This is a minimal change to the existing middleware. Rate limiting, format validation, and SEO headers continue to work unchanged.

- [ ] Task 5: Update `validateToken` in proposal-helpers.ts
  - File: `apps/web/src/lib/proposal-helpers.ts`
  - Action: Update the existing `validateToken` function to accept an optional `statuses` parameter (defaults to `['published']` for backward compat). The middleware will pass `['published', 'sent', 'viewed', 'draft']`. This keeps `getProposalBySlugAndToken` unchanged (still filters `status = 'published'` for old React rendering).
  - Notes: This ensures old proposals keep working via the React path while new proposals can be accessed in multiple statuses.

#### Phase 2: HTML Proxy Route

- [ ] Task 6: Move old proposal rendering to legacy sub-route
  - Files:
    - Move `apps/web/src/app/proposals/[slug]/[token]/page.tsx` → `apps/web/src/app/proposals/[slug]/[token]/legacy/page.tsx`
    - Move `apps/web/src/app/proposals/[slug]/[token]/layout.tsx` → `apps/web/src/app/proposals/[slug]/[token]/legacy/layout.tsx`
  - Action: Move existing page.tsx and layout.tsx into a `legacy/` subdirectory so they can coexist with the new `route.ts` in the parent directory. Update imports if any paths change. The layout.tsx should continue to handle branding CSS vars and header rendering for old proposals.
  - Notes: This is the cleanest way to handle the Next.js constraint that `route.ts` and `page.tsx` cannot coexist. Old proposals get internally redirected to `/proposals/{slug}/{token}/legacy` by the route handler (invisible to users).

- [ ] Task 7: Create HTML proxy route handler
  - File: `apps/web/src/app/proposals/[slug]/[token]/route.ts`
  - Action: Create GET route handler that:
    1. Receives `slug` and `token` from async params
    2. Queries `proposals` table via admin client for matching row (select `html_path`, `expires_at`, `asset_manifest`)
    3. **If row not found** → return 404
    4. **If `html_path` is NULL** (old React proposal) → return `NextResponse.rewrite(new URL(\`/proposals/${slug}/${token}/legacy\`, request.url))` to render via old React page
    5. **If `html_path` is set** (new HTML proposal):
       a. Check `expires_at` — if past, return `new Response('Proposal expired', { status: 410 })`
       b. Call `increment_view` RPC via `supabase.rpc('increment_view', { proposal_slug: slug })`
       c. Fetch HTML from Storage: `supabase.storage.from('proposals').download(html_path)`
       d. Call `injectSignedUrls(html, slug, assetManifest)`
       e. Return `new Response(html, { status: 200, headers: { 'Content-Type': 'text/html; charset=utf-8', 'X-Robots-Tag': 'noindex, nofollow', 'Cache-Control': 'private, no-cache', 'Content-Security-Policy': "default-src 'self' 'unsafe-inline'; img-src * data:; font-src *" } })`
  - Notes: The `NextResponse.rewrite()` for old proposals is an internal rewrite — the user's URL stays the same, but Next.js renders the `legacy/` route. This avoids reimplementing the entire React rendering pipeline inside the route handler. Export `dynamic = 'force-dynamic'` to prevent caching.

- [ ] Task 8: Create `injectSignedUrls` utility
  - File: `apps/web/src/lib/proposal-helpers.ts`
  - Action: Add function `injectSignedUrls(html: string, slug: string, assetManifest: AssetManifest): Promise<string>` that:
    1. Creates Supabase admin client
    2. For each placeholder, use the **exact path from `assetManifest`** (not guessed):
       - `{{logo:client}}` → `supabase.storage.from('proposal-assets').createSignedUrl(assetManifest.clientLogo, 3600)`
       - `{{logo:tractis}}` → `supabase.storage.from('proposal-assets').createSignedUrl(assetManifest.tractisLogo, 3600)`
       - `{{favicon:client}}` → if `assetManifest.clientFavicon` exists, create signed URL; otherwise remove the placeholder
    3. Replace placeholders in HTML string using exact string match (only `{{logo:client}}`, `{{logo:tractis}}`, `{{favicon:client}}` — these are intentionally distinct patterns unlikely to appear in content)
    4. Return processed HTML
  - Notes: Using the asset manifest eliminates the guessing problem (F13). Extensions are stored at upload time and reused at serve time.

- [ ] Task 9: Add `getHtmlProposal` helper
  - File: `apps/web/src/lib/proposal-helpers.ts`
  - Action: Add function `getHtmlProposal(slug: string, token: string): Promise<{ html: string; expired: boolean; assetManifest: AssetManifest } | null>` that:
    1. Queries `proposals` table via admin client for row matching slug + token where `html_path IS NOT NULL`
    2. Checks `expires_at` — if past, returns `{ html: '', expired: true, assetManifest: {} }`
    3. Calls `increment_view` RPC
    4. Fetches HTML from Storage bucket using `html_path`
    5. Returns `{ html, expired: false, assetManifest }` (signed URL injection happens in route handler)
  - Notes: Keeps existing `getProposalBySlugAndToken` untouched for old proposals. Does NOT call `injectSignedUrls` — that happens in the route handler so the asset manifest can be passed through.

#### Phase 3: Generation Pipeline Script

- [ ] Task 10: Create `scripts/package.json` for pipeline dependencies
  - File: `scripts/package.json`
  - Action: Create a minimal package.json for the scripts directory:
    ```json
    {
      "name": "proposal-scripts",
      "private": true,
      "type": "module",
      "dependencies": {
        "@anthropic-ai/sdk": "^0.39.0",
        "@supabase/supabase-js": "^2.95.0",
        "nanoid": "^5.1.0",
        "dotenv": "^16.4.0"
      }
    }
    ```
  - Notes: Scripts need their own dependencies since they are NOT part of a pnpm workspace package. Run `cd scripts && pnpm install` after creating this file. The `@anthropic-ai/sdk` is used directly (simpler than LangChain for sequential non-streaming calls in a script context).

- [ ] Task 11: Create `scrapeBrand` utility function
  - File: `scripts/lib/brand.ts`
  - Action: Create function `scrapeBrand(url: string): Promise<DembrandtResult>` that:
    1. Runs Dembrandt via `npx --prefix ../apps/agent dembrandt <url>` using `child_process.execSync` (60s timeout, 10MB buffer). This uses the version installed in `apps/agent` without requiring global installation.
    2. Parses stdout for hex colors via regex `/#([0-9a-fA-F]{6})/g`, deduplicates
    3. Extracts favicon URL via simple HTTP fetch of the URL + regex match on `<link[^>]*rel=["'](?:shortcut )?icon["'][^>]*href=["']([^"']+)["']` — NO Puppeteer needed (avoids 400MB Chromium dependency)
    4. Returns `{ colors: string[], favicon: string | null, raw: string }`
  - Notes: Favicon extraction uses a simple `fetch()` + HTML regex instead of Puppeteer. This covers 95% of cases. For sites with JS-rendered favicons, the `--favicon <url>` CLI override in the main script serves as fallback.

- [ ] Task 12: Create `uploadClientAssets` utility function
  - File: `scripts/lib/assets.ts`
  - Action: Create function `uploadClientAssets(slug: string, logoUrl: string, faviconUrl: string | null): Promise<AssetManifest>` that:
    1. Downloads logo from URL using `fetch`
    2. Determines extension from Content-Type header (e.g., `image/svg+xml` → `svg`, `image/png` → `png`)
    3. Uploads to `proposal-assets/{slug}/logo.{ext}` via Supabase Storage
    4. If favicon URL exists, downloads and uploads to `proposal-assets/{slug}/favicon.{ext}`
    5. Ensures Tractis logo exists at `proposal-assets/tractis/logo.svg` (check existence first, upload only if missing)
    6. **Returns `AssetManifest`** object: `{ clientLogo: '{slug}/logo.{ext}', clientFavicon: '{slug}/favicon.{ext}' | undefined, tractisLogo: 'tractis/logo.svg' }`
  - Notes: The returned manifest is stored in the DB `asset_manifest` column so `injectSignedUrls` knows exact paths. Uses `supabase.storage.from('proposal-assets').upload(path, buffer, { contentType, upsert: true })`.

- [ ] Task 13: Create `compileTailwind` utility function
  - File: `scripts/lib/tailwind.ts`
  - Action: Create function `compileTailwind(htmlContent: string): Promise<string>` that:
    1. Writes HTML to a temp file in `os.tmpdir()` (cross-platform)
    2. Creates a minimal empty CSS input file in `os.tmpdir()` (cross-platform replacement for `/dev/null`):
       ```
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```
    3. Runs **`npx tailwindcss -i <inputCssFile> --content <tempHtmlFile> --minify`** (NOT `@tailwindcss/cli` which is v4 only)
    4. Captures compiled CSS output (write to another temp file via `-o <outputFile>`, then read it)
    5. In the HTML: removes any `<script src="...tailwind...">` or `<script src="https://cdn.tailwindcss.com...">` tags via regex
    6. Injects compiled CSS as `<style>` block inside `<head>` (before `</head>`)
    7. Cleans up all temp files
    8. Returns self-contained HTML
  - Notes: Uses `npx tailwindcss` (Tailwind v3 CLI). No custom tailwind.config needed — agents must use ONLY default Tailwind utility classes (no custom theme extensions). The agent prompts (Tasks 15-18) must explicitly state this constraint. All custom styling should use inline CSS custom properties on `:root`, not Tailwind config extensions.

- [ ] Task 14: Create main `generate-proposal.ts` pipeline script
  - File: `scripts/generate-proposal.ts`
  - Action: Create CLI script that orchestrates the full pipeline:
    1. Parse CLI args: `--doc <path>` (proposal markdown), `--client <name>`, `--url <website>`, `--logo <url>` (optional override), `--favicon <url>` (optional override)
    2. Load env from `../apps/agent/.env` via dotenv (same pattern as `save-proposal.ts`)
    3. Generate slug from client name (lowercase, replace spaces with hyphens, strip non-alphanumeric)
    4. Generate token via `nanoid(10)`
    5. **Step 1**: `scrapeBrand(url)` → raw brand data. **On failure**: log warning, continue with empty colors (Agent 1 handles gracefully)
    6. **Step 2**: `uploadClientAssets(slug, logoUrl || extracted, faviconUrl || extracted)` → asset manifest
    7. **Step 3**: Call Agent 1 (Brand Designer) via Anthropic SDK — input: raw Dembrandt JSON → output: design system JSON. **Validate** output against `DesignSystemSchema`. **On failure**: retry once, then abort with error.
    8. **Step 4**: Call Agent 2 (Architect) via Anthropic SDK — input: design system + proposal markdown → output: section architecture JSON. **Validate** against `SectionBlueprintSchema` array. **On failure**: retry once, then abort.
    9. **Step 5**: Call Agent 3 (HTML Builder) via Anthropic SDK — input: architecture + design system + markdown → output: raw HTML. Set `max_tokens: 16384`. **Check for truncation**: if response `stop_reason !== 'end_turn'`, log warning. **On failure**: retry once, then abort.
    10. **Step 6**: Call Agent 4 (Polish) via Anthropic SDK — input: raw HTML → output: polished HTML. Set `max_tokens: 16384`. **On failure**: skip polish, use Agent 3 output (polish is optional improvement, not critical).
    11. **Step 7**: `compileTailwind(polishedHtml)` → self-contained HTML
    12. **Step 8**: Upload HTML to `proposals/{slug}.html` in Supabase Storage
    13. **Step 9**: Insert record in `proposals` table: `slug`, `token`, `client_name`, `client_url`, `status: 'draft'`, `html_path: '{slug}.html'`, `asset_manifest`
    14. **Step 10**: Print URL `https://proposal.tractis.ai/proposals/{slug}/{token}`
  - Notes: Each agent call reads its system prompt from `apps/agent/prompts/` via `fs.readFileSync`. Use `@anthropic-ai/sdk` directly (from `scripts/package.json`). Log progress for each step to stdout. Wrap entire pipeline in try/catch — on any unrecoverable failure, clean up uploaded assets and print clear error message.

#### Phase 4: Agent System Prompts

- [ ] Task 15: Create Agent 1 — Brand Designer prompt
  - File: `apps/agent/prompts/agent-1-brand-designer.md`
  - Action: Write system prompt that instructs Sonnet 4.5 to:
    - Input: Raw Dembrandt JSON (hex colors array, favicon URL, raw CSS snippet)
    - Job: Filter UI noise (grays, state colors, third-party colors), identify 2-3 real brand colors from logo/header/CTAs, generate complete design system
    - Output: JSON matching `DesignSystemSchema` exactly
    - Rules: Never use pure white (#fff) or pure black (#000) for backgrounds — always tint with brand. All surface colors must be brand-tinted.
    - Graceful degradation: If input has zero colors, use professional defaults (navy + gold) and note in output.
  - Notes: This is the "taste" agent — its output determines the visual quality of the entire proposal.

- [ ] Task 16: Create Agent 2 — Proposal Architect prompt
  - File: `apps/agent/prompts/agent-2-architect.md`
  - Action: Write system prompt that instructs Sonnet 4.5 to:
    - Input: Design system JSON + complete proposal markdown
    - Job: Analyze content and design unique visual structure. Each proposal gets a DIFFERENT layout based on its content.
    - Output: JSON array of sections matching `SectionBlueprintSchema[]`
    - Visual pattern mapping rules: metrics → number tickers/bento grid, timeline → tracing beam, before/after → split section, pricing → cards with highlight, technical → sobrio/code blocks, executive → more visual/less text
    - Available patterns (from REFACTOR_SPEC visual library): spotlight cards, background beams, floating navbar, typewriter text, lamp headers, wavy backgrounds, 3D hover cards, meteors, tracing beams, animated gradients, marquee, shimmer buttons, bento grids, blur fade, cinematic heroes, asymmetric bento, vertical timelines, pricing tables, split sections, parallax, sticky headers
  - Notes: This agent decides the "architecture" — what the proposal looks like, not the content. Content comes from the markdown.

- [ ] Task 17: Create Agent 3 — HTML Builder prompt
  - File: `apps/agent/prompts/agent-3-html-builder.md`
  - Action: Write system prompt that instructs Sonnet 4.5 to:
    - Input: Architecture JSON + Design system JSON + proposal markdown
    - Job: Follow the architect's blueprint exactly. Generate complete HTML document with Tailwind utility classes, CSS animations, vanilla JS (IntersectionObserver for scroll reveals).
    - Output: Complete HTML document (`<!DOCTYPE html>` to `</html>`)
    - Rules:
      - ALL proposal content must be present — no summaries, no omissions
      - Mobile responsive (Tailwind breakpoints: sm, md, lg, xl)
      - Use asset placeholders: `{{logo:client}}`, `{{logo:tractis}}`, `{{favicon:client}}`
      - Include Tailwind CDN `<script src="https://cdn.tailwindcss.com">` tag (will be stripped during compilation)
      - **Use ONLY default Tailwind utility classes** — no custom theme extensions (no `text-brand-primary`, no `bg-custom-*`). Custom colors must be applied via inline CSS custom properties on `:root` and referenced in `style` attributes, NOT via Tailwind config.
      - Animations via CSS `@keyframes` + `animation` properties (in a `<style>` block)
      - Scroll reveals via `IntersectionObserver` in a single `<script>` block
      - No external dependencies (no React, no npm packages, no external JS/CSS CDNs besides Tailwind)
      - Semantic HTML (sections, articles, headers, footers)
    - **Output size**: Aim for 800-1500 lines. If the proposal content is very long, prioritize completeness over visual flourish.
  - Notes: This is the largest agent output. `max_tokens: 16384` will be set by the pipeline script.

- [ ] Task 18: Create Agent 4 — Polish & QA prompt
  - File: `apps/agent/prompts/agent-4-polish.md`
  - Action: Write system prompt that instructs Sonnet 4.5 to:
    - Input: Complete HTML from Agent 3
    - Job: Make it premium. Fix visual issues WITHOUT changing content or structure.
    - Output: Polished HTML (same structure, refined visuals)
    - Checklist:
      - Consistent spacing (no abrupt jumps between sections)
      - Typography hierarchy (heading sizes follow logical scale)
      - Gradient and shadow consistency (all use brand tints from `:root` vars)
      - Smooth transitions between sections (no hard color boundaries)
      - Micro-interactions (hover states, subtle transforms)
      - Mobile responsive verification (no overflow, readable text)
      - Smooth scroll behavior (`scroll-behavior: smooth` on html)
      - Favicon and logo placeholders present (`{{logo:client}}`, `{{logo:tractis}}`, `{{favicon:client}}`)
      - No broken placeholder references
      - **Verify only default Tailwind classes are used** — flag any custom classes that won't compile
    - **Do NOT add external scripts, stylesheets, or dependencies**
    - **Do NOT change content text or section order**
  - Notes: This agent is optional — if it fails, the pipeline uses Agent 3 output directly. It should NOT add content or change structure, only visual refinements.

### Acceptance Criteria

- [ ] AC 1: Given the new migration is applied, when querying the `proposals` table, then it has columns `client_name`, `client_url`, `view_count`, `expires_at`, `html_path`, `asset_manifest` alongside existing `client`, `metadata`, `blocks` columns.
- [ ] AC 2: Given Supabase Storage is set up, when calling `supabase.storage.from('proposals').list()`, then the bucket exists and is private.
- [ ] AC 3: Given an HTML proposal exists in Storage with a valid DB record, when visiting `/proposals/{slug}/{token}`, then the browser receives raw HTML (not React-rendered) with `Content-Type: text/html` and a CSP header.
- [ ] AC 4: Given an HTML proposal with asset placeholders, when the proxy serves it, then `{{logo:client}}` and `{{logo:tractis}}` are replaced with valid signed URLs (using paths from `asset_manifest`) that resolve to images.
- [ ] AC 5: Given an old JSON-based proposal exists (no `html_path`), when visiting its `/proposals/{slug}/{token}` URL, then it still renders via React at the `/legacy` sub-route (internally rewritten, URL unchanged for user).
- [ ] AC 6: Given an HTML proposal with `expires_at` in the past, when visiting its URL, then the proxy returns a 410 Gone response.
- [ ] AC 7: Given a valid proposal markdown and client URL, when running `npx tsx scripts/generate-proposal.ts --doc proposal.md --client "Acme" --url "https://acme.com"`, then the script completes all steps and outputs a working proposal URL.
- [ ] AC 8: Given Agent 3 outputs HTML with Tailwind CDN and utility classes, when `compileTailwind()` runs on Windows, then the output has no `<script>` CDN tag and has an inline `<style>` block with compiled CSS. No `/dev/null` or Unix-only paths used.
- [ ] AC 9: Given a new proposal is created, when checking the DB record, then `view_count` is 0, `status` is `'draft'`, and `asset_manifest` contains valid Storage paths.
- [ ] AC 10: Given a proposal with `status: 'sent'` is viewed, when the proxy serves it, then `view_count` increments by 1 and `status` changes to `'viewed'`.
- [ ] AC 11: Given Agent 3 fails on the first attempt, when the pipeline retries, then it makes one retry attempt before aborting with a clear error message.
- [ ] AC 12: Given the middleware receives a request for a proposal with `status: 'sent'`, when it validates the token, then the request is allowed through (not blocked by RLS or status filter).

## Additional Context

### Dependencies

- **scripts/package.json** (new):
  - `@anthropic-ai/sdk` — Direct Anthropic API calls for the 4-agent pipeline
  - `@supabase/supabase-js` — Storage and DB operations
  - `nanoid` — Token generation (10-char)
  - `dotenv` — Load env from `apps/agent/.env`
- **npx tailwindcss** (Tailwind v3 CLI) — Used via npx in `compileTailwind()`. Uses the project's installed version. No `@tailwindcss/cli` package needed.
- **Supabase CLI** — Already configured for local dev. Migrations run via `supabase db push` or `supabase migration up`.
- **Dembrandt** — Used via `npx --prefix apps/agent dembrandt` (from `apps/agent` dependency). NOT globally installed.

### Testing Strategy

**Manual testing (primary for this refactor):**
- Create a test proposal using `generate-proposal.ts` against a known client website
- Verify HTML renders correctly at the proposal URL on both desktop and mobile
- Verify asset signed URLs resolve to actual images
- Verify old JSON proposals still render via React at the same URL (internally rewritten to `/legacy`)
- Verify expired proposal returns 410
- Verify view count increments on access (check for reasonable counting — multiple refreshes will increment)
- Verify CSP header is present in response
- Run `compileTailwind()` on Windows to verify cross-platform compatibility

**Unit tests:**
- `injectSignedUrls()` — mock Supabase Storage, verify placeholder replacement using asset manifest paths
- `compileTailwind()` — verify CDN script removal, style injection, and temp file cleanup on Windows
- `scrapeBrand()` — mock `child_process.execSync` output, verify color parsing and favicon regex

**Integration test:**
- Run `generate-proposal.ts` end-to-end against local Supabase with a sample markdown file
- Verify DB record created with correct `asset_manifest`, HTML uploaded, URL accessible

### Notes

**High-risk items:**
- **Agent output quality** — Agents may produce HTML that doesn't match the design system or has visual bugs. Agent 4 (Polish) mitigates this but isn't guaranteed. Manual review of first 5-10 proposals is critical.
- **Tailwind compilation** — Agents must use ONLY default Tailwind utility classes. If they invent custom classes (e.g., `text-brand-primary`), those will silently disappear during compilation. The agent prompts explicitly forbid this, but LLMs may not always comply. Visual review is essential.
- **Large HTML output** — Agent 3 may produce very large HTML (2000+ lines). `max_tokens: 16384` allows ~12,000 tokens of HTML output. If truncated (`stop_reason !== 'end_turn'`), the pipeline logs a warning. For very long proposals, content may need to be split across multiple agent calls (out of scope for v1).
- **View count inflation (F9)** — `increment_view` fires on every request including page refreshes. This is acceptable for v1 — the count represents "page loads" not "unique views." If unique views are needed later, add session-based deduplication (out of scope).
- **HTML security (F18)** — LLM-generated HTML is served directly. CSP headers (`default-src 'self' 'unsafe-inline'`) prevent loading external scripts but allow inline styles/scripts (needed for animations). Proposal markdown comes from Gerhard (trusted), not external users, so prompt injection risk is low. If external markdown sources are added later, HTML sanitization should be revisited.

**Findings addressed from adversarial review:**
- F1 (Critical): Fixed — use `npx tailwindcss` not `@tailwindcss/cli@3`
- F2 (Critical): Fixed — move old page.tsx to `legacy/` sub-route, use `NextResponse.rewrite()`
- F3 (Critical): Fixed — middleware switches to admin client (bypasses RLS), RLS policy also updated for defense-in-depth
- F4 (High): Fixed — `scripts/package.json` with own `nanoid` dependency
- F5 (High): Fixed — no Puppeteer in scripts; favicon via simple HTTP fetch + regex
- F6 (High): Fixed — run Dembrandt via `npx --prefix apps/agent`
- F7 (High): Fixed — agents instructed to use only default Tailwind utilities; custom colors via `:root` CSS vars
- F8 (High): Fixed — temp files instead of `/dev/null`; cross-platform approach
- F9 (Medium): Acknowledged — view count = page loads, not unique views (acceptable for v1)
- F10 (Medium): Fixed — `getProposalBySlugAndToken` unchanged; new `getHtmlProposal` uses admin client with no status filter
- F11 (Medium): Fixed — removed specific "22" count reference
- F12 (Medium): Fixed — retry logic (1 retry per agent), Agent 4 failure is non-fatal, truncation detection
- F13 (Medium): Fixed — `asset_manifest` JSONB column stores exact paths with extensions
- F14 (Medium): Fixed — buckets created via Dashboard/CLI, not raw SQL
- F15 (Medium): Fixed — using `@anthropic-ai/sdk` in scripts (appropriate for non-streaming sequential calls); LangChain stays in apps/agent for its own use
- F16 (Low): Fixed — version reference corrected to "Tailwind CSS 3.4"
- F17 (Low): Acknowledged — acceptable behavior; middleware validates access, route handler checks expiry
- F18 (Low): Fixed — CSP headers added to HTML response
- F19 (Low): Fixed — shared types moved to Phase 0 (prerequisite for Phase 3)

**Future considerations (out of scope):**
- Admin dashboard showing HTML proposals with preview thumbnails
- PDF export from HTML proposals
- A/B testing different visual patterns per proposal
- Unique view tracking (session-based deduplication)
- Analytics beyond view count (time on page, scroll depth)
- Removing old React block components and JSONB columns after migration is proven
- HTML sanitization for untrusted markdown sources
